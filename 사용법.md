# 균열폭 측정기 웹앱 — 사용법

브라우저 안에서만 동작하는 콘크리트 균열폭 측정 웹앱입니다.
서버가 필요 없고, 사진은 어디로도 전송되지 않습니다.

## 구성 파일
- `index.html` — 화면과 스타일
- `app.js` — 측정 로직 (동전 검출, 균열폭 측정)

두 파일은 항상 **같은 폴더**에 함께 있어야 합니다.

## 실행 방법

### 방법 1) 내 PC에서 바로 열기
`index.html`을 더블클릭하면 브라우저에서 열립니다.
(단, 일부 브라우저는 로컬 파일에서 카메라 접근을 제한할 수 있습니다.)

### 방법 2) 폰에서 쓰기 — 무료 배포 (권장)
현장에서 폰으로 쓰려면 웹에 올려야 합니다. 서버가 필요 없는 정적 사이트라
아래 무료 서비스에 파일 2개만 올리면 링크가 생깁니다:

- **GitHub Pages**: 저장소에 두 파일을 올리고 Pages 기능 켜기
- **Netlify Drop** (가장 간단): https://app.netlify.com/drop 에
  폴더를 드래그&드롭하면 즉시 링크 생성
- **Cloudflare Pages**, **Vercel** 등도 동일

생성된 링크를 폰 브라우저에서 열면, 홈 화면에 추가해 앱처럼 쓸 수 있습니다.

## 사용 순서
1. 동전 지름을 확인 (100원 = 24.0mm, 기본값)
2. 사진을 드래그&드롭하거나 탭해서 촬영/선택 (여러 장 가능)
3. 자동으로 동전을 찾아 스케일을 잡고 균열폭을 측정
4. 결과 표에서 각 사진의 최대·평균·95%·중앙 폭 확인
5. **"보기"를 눌러 동전과 균열이 제대로 잡혔는지 반드시 검증**
6. "CSV로 내보내기"로 엑셀에서 열 수 있는 파일 저장

## 촬영 요령 (정확도의 핵심)
- 동전을 균열과 **같은 벽면에 밀착**시켜 촬영
- **손가락으로 동전을 눌러 잡아도 됩니다.** 보이는 테두리로 크기를 복원하므로
  둘레의 60% 이상만 보이면 안정적으로 측정됩니다. 손가락은 동전 한쪽만
  누르고 테두리 대부분이 보이게 하세요.
- 카메라를 벽면에 **수직**으로 (비스듬하면 스케일 왜곡)
- 동전과 균열을 화면 중앙 부근에, 초점을 정확히
- 동전이 타원으로 찍히지 않게 정면에서

## 동전 검출 방식 (부분 원 피팅)
전체 원을 요구하지 않습니다. 동전의 보이는 테두리 점들을 모아
최소제곱 원 피팅 + RANSAC(이상점 제거)으로 원을 복원합니다.
손가락에 가려진 부분은 나머지 테두리로 수학적으로 채워집니다.
검증 테스트에서 둘레의 40%만 보여도 반경 오차 0.3px 수준이었습니다.
“보기” 화면의 파란 점선 원이 복원된 동전 크기입니다 —
이 원이 실제 동전 테두리와 맞는지 확인하세요.

## 한계와 주의
- 자동 검출은 조명·각도·반사에 영향을 받아 오차가 생깁니다.
- 형태학적 처리로 폭이 실제보다 약간 크게 나올 수 있습니다.
- **구조 안전성 판정용이 아니라 현장 스크리닝용**입니다.
  중요한 균열은 균열 게이지로 교차 검증하고 전문가 판단을 받으세요.

## 정확도를 더 높이려면
- 동전 검출을 Hough 대신 컨투어+타원피팅으로 바꾸면 서브픽셀 정밀도 확보
- 원근 보정(호모그래피) 추가 시 비스듬한 촬영도 교정 가능
